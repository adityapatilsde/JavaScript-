//math - built in object that provides a collection of properties and methods
//console.log(Math.PI);
//console.log(Math.E);
let x = 3.21;
let y = 2;
let z;

z = Math.round(x);
console.log(z);
//Math.round it does the round off of the number with normal rules of rounding off like depenmding on greater than or less than half of value
//======================================================================================================================

let a = 3.99;
b = Math.floor(a);
console.log(b);

//Math.florr here the value gets  in smallest possible rounding value like 3.65=3 , 3.78=3 and 3.44=3
//==========================================================================================

let ab = 3.12;
ba = Math.ceil(ab);
console.log(ba);

//Math.ceil here we get the nearest highest possible rounding value like 3.14=4 ,3.76=4,3.22=4
//==============================================================================================

let k = 3.21;
let o = Math.trunc(z);
console.log(y);

//removes fractional digits if x is already an integer
//====================================================================================================

let xo = 3;
let ox = 2;
let oo;
oo = Math.pow(xo,ox);
console.log(oo);

//Returns the value of a base expression taken to a specified power
//========================================================================================================

let ro = 81;
let op;
op = Math.sqrt(ro);
console.log(op);

//Returns the square root of a number.
//===============================================================================================================

m = 10
j = Math.log(m);
console.log(j);

//Returns the natural logarithm (base e) of a number.
//=============================================================================================================

bg = 45;
fg = Math.sin(bg);
gh = Math.cos(bg);
console.log(fg,gh);

//Math.sin Returns the sine of a number
//Math.cos Returns the cosin of a number 
//=====================================================================================================
